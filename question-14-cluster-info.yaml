#1. Only one master node is available                                                                   | kubectl get nodes
#2. There are two worker nodes available                                                                | kubectl get nodes
#3. 192.168.0.0/24                                                                                      | kubectl describe node | less -p PodCIDR
#4. - --service-cluster-ip-range=10.96.0.0/12                                                           | cat /etc/kubernetes/manifests/kube-apiserver.yaml | grep range 
#5. "name": "k8s-pod-network","cniVersion": "0.3.1", "plugins": [{"type": "calico","log_level": "info", | cd /etc/cni/net.d/10-calico.conflist 
#6. on worker node 2
